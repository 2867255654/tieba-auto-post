# 本地定时任务（Windows）部署说明

之前的 GitHub Actions 方案用来**采集 + 生成草稿**没问题，但**真发帖**失败（`TooManyRedirects`）——
因为 GitHub 的服务器在**境外**，百度对境外 IP + BDUSS 直接循环跳安全校验页。

**真发帖必须在你自己电脑上跑**：你的 IP 在国内、是百度信任的登录环境，风控/重定向问题基本消失。

本目录的 `run_local.bat` 就是为此准备的。下面的步骤把它挂到 Windows 任务计划，实现「每天自动跑、自动发帖」。

---

## 一、填配置（先别开真发）

编辑同目录的 **`.env`** 文件：

| 项 | 填什么 | 说明 |
|---|---|---|
| `BDUSS` | 浏览器 F12 复制的 BDUSS 值 | 真发必填；用专用小号，别用主号 |
| `POST_MODE` | 先填 `0` | 先跑几次草稿验证；确认无误再改 `1` |
| `FORUM_NAMES` | `抗压背锅吧,英雄联盟吧,...` | 想发哪些吧列哪些，逗号分隔 |
| `FORUM_PICK_ALL` | 填 `0` | 每次随机选 1 个吧发（推荐）；`1`= 全发 |
| `RSSHUB_BASE` | 自建 RSSHub 地址 | 不填则国内源 3DM/游民/小黑盒拉不到 |

> 草稿模式（`POST_MODE=0`）下 `BDUSS` 留空也能跑，方便先验证采集和生成。

---

## 二、先双击试跑一次

双击 `run_local.bat`：
- 黑窗口会打印采集条数、标题、草稿保存路径
- 默认 `POST_MODE=0` → 只生成 `output/draft_*.txt`，**不会发帖**
- 去 `output/` 看草稿内容是否满意

---

## 三、挂到 Windows 任务计划（每天自动跑）

1. 按 `Win` 键，输入 **任务计划程序**，回车打开
2. 右侧 **创建基本任务**：
   - **名称**：`贴吧游戏资讯每日摘抄`
   - **触发器**：`每天` → 设定时间（如 08:00，可勾「每隔 1 天」）
   - **操作**：`启动程序`
     - **程序或脚本**：浏览选 `run_local.bat`（在 `tieba_game_digest` 目录里）
     - **起始于(可选)**：填 `run_local.bat` 所在目录，例如
       `C:\Users\yuanliang\WorkBuddy\2026-09-23-17-40-44\tieba_game_digest`
   - 完成前勾 **「打开此任务属性的对话框」**
3. 在属性里：
   - **常规** → 勾 **「不管用户是否登录都要运行」** + **「使用最高权限运行」**（否则后台跑可能读不到 .env / 网络受限）
   - 下方 **配置为 Windows 10/11**
   - **条件** → 取消「只有在计算机使用交流电源时才启动此任务」（笔记本防睡死）
   - **设置** → 勾「如果过了计划时间，立即运行」
4. 确定。会让你输一次 Windows 登录密码（用于后台运行权限），照输即可。

### 想一天跑两次？
再建一个同名任务（或复制），触发器加一条「每天 20:00」。

### 手动测试任务是否生效
任务计划程序里右键该任务 → **运行**，立刻跑一次；看 `output/` 是否出新草稿。

---

## 四、开真发（确认无误后）

1. 编辑 `.env`，把 `POST_MODE=0` 改成 `POST_MODE=1`
2. 确保 `BDUSS` 已填且是**专用小号的最新值**
3. 双击 `run_local.bat` 先手动跑一次，去对应吧看帖子在不在、有没有秒删/弹验证码
4. 正常后让任务计划每天自动跑

### 出问题立刻停
发现被删帖/封禁，把 `.env` 的 `POST_MODE` 改回 `0` 即可，任务计划就不会再发。

---

## 五、和 GitHub Actions 怎么分工

| 功能 | 用哪个 | 说明 |
|---|---|---|
| 每天采集 + 生成草稿 | GitHub Actions **或** 本地任务 | 二选一即可；Actions 在境外拉国内源偏慢 |
| 真发帖 | **只能本地任务** | 境外 IP 会被贴吧重定向/风控 |
| 多吧随机发 | 两者都支持 | `FORUM_PICK_ALL=0` |

如果你只想在本地跑全套（采集+发帖），可以**把 GitHub 上的定时关掉**（仓库 Settings → Actions → 禁用 workflow），避免重复跑。

---

## 六、风险提示（务必看）

- 贴吧协议 4.1.5 明确禁止发贴机/批量刷帖；即便随机单吧，仍有封号风险。
- **用专用小号**，主号千万别上。
- 低频：每天 1 次、每个吧 1 帖；内容差异化能进一步降风险（进阶可让每个吧生成不同标题）。
- `BDUSS` 等同账号钥匙，本机 `.env` 文件别分享、别提交。
